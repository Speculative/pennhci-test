# Generated by /carthage-annex. Safe to edit; re-running the skill will
# offer to diff before overwriting.
[carthage]
schema_version = 1                          # config TOML schema version
template_version = 1                        # annex template bundle version
base_image_tag = "v2"            # carthage-base major this project targets
service_name = "dev"
project_slug = "pennhci-test"
